# --- Web layer ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20         # multipart/form-data file uploads

# --- Config & validation ---
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- Database (MySQL) ---
SQLAlchemy==2.0.36
PyMySQL==1.1.1
cryptography==44.0.0             # required by PyMySQL for caching_sha2_password

# --- Authentication ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt is pinned BELOW 4.1 on purpose. passlib 1.7.4 reads bcrypt.__about__
# to detect the backend version, and bcrypt removed that attribute in 4.1, so
# newer versions make passlib log a spurious "error reading bcrypt version"
# traceback on the first hash. It still works, but a scary traceback on every
# cold start is not something to ship.
bcrypt==4.0.1

# --- AI ---
groq==0.15.0
langgraph==0.2.60

# --- Document parsing ---
PyMuPDF==1.25.1                  # imported as `fitz`, used for PDF text
python-docx==1.1.2               # .docx
# .txt and .eml are handled with the standard library (email.parser)
